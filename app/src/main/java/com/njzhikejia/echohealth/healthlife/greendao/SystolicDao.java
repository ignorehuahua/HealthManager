package com.njzhikejia.echohealth.healthlife.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.njzhikejia.echohealth.healthlife.entity.rule.Systolic;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "SYSTOLIC".
*/
public class SystolicDao extends AbstractDao<Systolic, Void> {

    public static final String TABLENAME = "SYSTOLIC";

    /**
     * Properties of entity Systolic.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Min = new Property(0, double.class, "min", false, "MIN");
        public final static Property Max = new Property(1, double.class, "max", false, "MAX");
    }


    public SystolicDao(DaoConfig config) {
        super(config);
    }
    
    public SystolicDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"SYSTOLIC\" (" + //
                "\"MIN\" REAL NOT NULL ," + // 0: min
                "\"MAX\" REAL NOT NULL );"); // 1: max
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"SYSTOLIC\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Systolic entity) {
        stmt.clearBindings();
        stmt.bindDouble(1, entity.getMin());
        stmt.bindDouble(2, entity.getMax());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Systolic entity) {
        stmt.clearBindings();
        stmt.bindDouble(1, entity.getMin());
        stmt.bindDouble(2, entity.getMax());
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public Systolic readEntity(Cursor cursor, int offset) {
        Systolic entity = new Systolic( //
            cursor.getDouble(offset + 0), // min
            cursor.getDouble(offset + 1) // max
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Systolic entity, int offset) {
        entity.setMin(cursor.getDouble(offset + 0));
        entity.setMax(cursor.getDouble(offset + 1));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(Systolic entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(Systolic entity) {
        return null;
    }

    @Override
    public boolean hasKey(Systolic entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
