package com.njzhikejia.echohealth.healthlife.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.njzhikejia.echohealth.healthlife.entity.rule.Pulse;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PULSE".
*/
public class PulseDao extends AbstractDao<Pulse, Void> {

    public static final String TABLENAME = "PULSE";

    /**
     * Properties of entity Pulse.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Min = new Property(0, double.class, "min", false, "MIN");
        public final static Property Max = new Property(1, double.class, "max", false, "MAX");
    }


    public PulseDao(DaoConfig config) {
        super(config);
    }
    
    public PulseDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PULSE\" (" + //
                "\"MIN\" REAL NOT NULL ," + // 0: min
                "\"MAX\" REAL NOT NULL );"); // 1: max
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PULSE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Pulse entity) {
        stmt.clearBindings();
        stmt.bindDouble(1, entity.getMin());
        stmt.bindDouble(2, entity.getMax());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Pulse entity) {
        stmt.clearBindings();
        stmt.bindDouble(1, entity.getMin());
        stmt.bindDouble(2, entity.getMax());
    }

    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    @Override
    public Pulse readEntity(Cursor cursor, int offset) {
        Pulse entity = new Pulse( //
            cursor.getDouble(offset + 0), // min
            cursor.getDouble(offset + 1) // max
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Pulse entity, int offset) {
        entity.setMin(cursor.getDouble(offset + 0));
        entity.setMax(cursor.getDouble(offset + 1));
     }
    
    @Override
    protected final Void updateKeyAfterInsert(Pulse entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    @Override
    public Void getKey(Pulse entity) {
        return null;
    }

    @Override
    public boolean hasKey(Pulse entity) {
        // TODO
        return false;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
